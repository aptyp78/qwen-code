# Ворота перед отправкой. Проверяют всю дельту форка, а не только последний
# коммит: незарегистрированное вторжение могло приехать в любом из них.
node scripts/aso/cli.js check || exit 1
node scripts/aso/cli.js drift || exit 1
