# Приёмка продукта: обязанность как машина

Проверено на живом прогоне 26.08.2026.

## Что проверялось

Утверждение: в текущей оркестрации **нет приёмки продукта**. «Цель достигнута»
означает ровно одно — модель перестала звать инструменты.

```
/** Why the loop ended. null = normal text completion (no tool calls). */
this.terminateMode = result.terminateMode ?? AgentTerminateMode.GOAL;
```

`runtime/agent-headless.ts:379`

## Результаты

| Проверка                                                      | Результат                           |
| ------------------------------------------------------------- | ----------------------------------- |
| Обязанность в определении агента доходит и выполняется        | да                                  |
| Журнал деятельности пишется, включая синхронных субагентов    | да, `agent-<id>.jsonl` + meta       |
| Из журнала извлекается акт: цель, операции, материал, продукт | да                                  |
| Невыполнимая задача (запрошенных данных не существует)        | **`status: completed`**             |
| Цикл приёмки существует в машине                              | **да, `tools/agent/agent.ts:1824`** |
| Блокирующее решение хука возвращает агента к работе           | да, проверено                       |

## Как работает цикл приёмки

`runSubagentStopHookLoop`: если хук вернул `decision: "block"`, его `reason`
подаётся агенту как новое задание, и агент исполняется заново. Защита от
зацикливания — `getStopHookBlockingCap`.

Механизм реализован полностью и по умолчанию не используется: хука нет, цикл
делает один оборот и выходит.

## Живой прогон

Субагент **без обязанности в системном промпте**. Обязанность задана только хуком.

```
попытка 1  выполнено=false  → заблокировано, причина подана агенту
попытка 2  выполнено=true   → принято
```

Агент дописал требуемое и сдал работу со второго раза.

## Подключение

`.qwen/settings.json`:

```json
{
  "hooks": {
    "SubagentStop": [
      {
        "hooks": [
          { "type": "command", "command": "node .qwen/hooks/priyomka.js" }
        ]
      }
    ]
  }
}
```

## Известные слабости

- Проверка идёт по подстроке. В прогоне агент написал `НЕРАЗГЛАШЕНО/НЕРАЗЛИЧЕНО` —
  регулярное выражение поймало, но проверка грубая и на других формулировках
  пропустит.
- **Обязанность не записана в журнал.** Постфактум по журналу нельзя установить,
  какая обязанность была у агента и выполнена ли она: в журнале есть задание,
  операции, материал и продукт, но нет системного промпта.

## Где лежит

Рабочий хук — `.qwen/hooks/priyomka.js`. Не в `aso/srt/`, потому что ESLint
проекта даёт node-глобалы только под `scripts/`, а `scripts/aso/**` — зона
`aso-meta`, куда агент не пишет без явного акта владельца. `.qwen/hooks/`
линтером игнорируется и является штатным местом для хуков.

Это тот случай, когда правила форка сработали как задумано: агент упёрся в
ворота и не стал их править, а нашёл место, подходящее по существу.
